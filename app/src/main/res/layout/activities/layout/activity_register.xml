<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="fill_parent"
    android:layout_height="fill_parent"
    xmlns:tools="http://schemas.android.com/tools"
    android:background="#fff"
    android:orientation="vertical"
    tools:context=".RegisterActivity"
    >

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_alignParentLeft="true"
        android:layout_alignParentStart="true"
        android:layout_centerInParent="true"
        android:layout_marginTop="84dp"
        android:orientation="vertical"
        android:paddingLeft="20dp"
        android:paddingRight="20dp"
        android:paddingTop="20dp">

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:background="@drawable/rectangle_button"
            android:orientation="vertical">

            <custom.RobotoEditText
                android:id="@+id/editText_email"
                android:layout_width="match_parent"
                android:layout_height="64dp"
                android:layout_marginBottom="10dp"
                android:drawableLeft="@drawable/ic_user_avatar_icon"
                android:drawableStart="@drawable/ic_user_avatar_icon"
                android:background="@drawable/rectangle_button_dark"
                android:drawablePadding="9dp"
                android:hint="Email"
                android:padding="10dp"
                android:inputType="text"
                android:textColor="@color/green_800"
                android:textColorHint="@color/primary_text"/>
        </LinearLayout>


        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:background="@drawable/rectangle_button"
            android:orientation="vertical">

            <custom.RobotoEditText
                android:id="@+id/editText_password"
                android:layout_width="match_parent"
                android:layout_height="64dp"
                android:drawableLeft="@drawable/ic_user_lock_icon"
                android:layout_marginBottom="10dp"
                android:hint="Password"
                android:drawablePadding="9dp"
                android:background="@drawable/rectangle_button"
                android:padding="10dp"
                android:textColor="@color/primary_text"
                android:textColorHint="@color/primary_text"
                />
        </LinearLayout>

        <!-- Login Button -->



        <Button
            android:id="@+id/button_register_user"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="20dip"
            android:background="@drawable/rounded_button"
            android:shadowColor="@android:color/white"
            android:text="@string/registerPage_RegisterText"
            android:textAlignment="center"
            android:textColor="@android:color/white" />





    </LinearLayout>

</RelativeLayout>

